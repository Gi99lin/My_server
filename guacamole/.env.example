# PostgreSQL credentials for Guacamole's internal DB.
# These are container-to-container only; not exposed externally.
# Generate a long random value before first deploy:
#   openssl rand -hex 24
POSTGRES_DB=guacamole_db
POSTGRES_USER=guacamole_user
POSTGRES_PASSWORD=replace_me_with_random_value
